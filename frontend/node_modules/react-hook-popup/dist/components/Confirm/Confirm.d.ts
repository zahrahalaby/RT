import React from 'react';
import './confirm.css';
export interface ConfirmProps {
    confirm: () => void;
    cancel: () => void;
    children?: React.ReactNode;
}
export declare const Confirm: ({ confirm, cancel, children }: ConfirmProps) => JSX.Element;
